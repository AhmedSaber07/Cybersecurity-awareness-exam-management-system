@model ExamManagementApp.Dtos.ManagerLogInDto

@{
    ViewData["Title"] = "LogIn";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@if (ViewBag.ManagerNotFound != null)
{
    <div class="alert alert-danger alert-dismissible fade show" role="alert">
        <strong>Error!</strong> @ViewBag.ManagerNotFound
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
}
<div class="row justify-content-center">
    <div class="col-md-4">
        <form asp-action="LogIn" class="mt-5">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group mt-2">
                <span asp-validation-for="Email" class="text-danger"></span>
                <label asp-for="Email" class="control-label"></label>
                <input asp-for="Email" class="form-control" />
            </div>
            <div class="form-group mt-2">
                <span asp-validation-for="Password" class="text-danger"></span>
                <label asp-for="Password" class="control-label"></label>
                <input asp-for="Password" class="form-control" />
            </div>
            <div class="form-group d-flex flex-column mt-4">
                <input type="submit" value="Submit" class="btn btn-primary" />
                <a asp-action="Register" class="align-self-center mt-2">don't have an account..?</a>
            </div>
        </form>
    </div>
</div>


